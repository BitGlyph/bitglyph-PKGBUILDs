# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-development-tools-meta
pkgver=1
pkgrel=1
pkgdesc="Build tooling. Mostly here because paru/AUR building and package compilation need them (base-devel-adjacent)."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  gcc
  fakeroot
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
