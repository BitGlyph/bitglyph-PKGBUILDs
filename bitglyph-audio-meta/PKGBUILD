# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-audio-meta
pkgver=1
pkgrel=1
pkgdesc="Sound stack — PipeWire and its compatibility layers."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  alsa-utils
  pipewire
  pipewire-alsa
  pipewire-jack
  pipewire-pulse
  wireplumber
  rtkit
  realtime-privileges
  sof-firmware
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
