# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-filesystems-storage-meta
pkgver=1
pkgrel=1
pkgdesc="Core storage-stack tooling expected to matter on the *installed* system (root/EFI filesystem types, volume management, encryption partners). Exotic/rarely-root filesystem support lives under live_install > filesystem_type_support instead."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  parted
  gptfdisk
  dosfstools
  btrfs-progs
  e2fsprogs
  xfsprogs
  lvm2
  mdadm
  zfs-utils
  bcachefs-tools
  ntfs-3g
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
