# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-base-system-meta
pkgver=1
pkgrel=1
pkgdesc="Absolute core packages needed regardless of desktop environment or use case. If you strip everything else, this is what's left."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  base
  bash-completion
  sudo
  polkit
  archlinux-keyring
  cachyos-keyring
  cachyos-mirrorlist
  cachyos-v3-mirrorlist
  cachyos-v4-mirrorlist
  cachyos-rate-mirrors
  cachyos-settings
  xdg-utils
  xdg-user-dirs-gtk
  mkinitcpio
  paru
  power-profiles-daemon
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
