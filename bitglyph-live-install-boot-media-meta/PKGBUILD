# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-live-install-boot-media-meta
pkgver=1
pkgrel=1
pkgdesc="Boot chain for the live medium itself (not the installed system's bootloader)."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  syslinux
  edk2-shell
  memtest86+
  memtest86+-efi
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
