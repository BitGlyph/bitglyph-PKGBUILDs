# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-kernel-meta
pkgver=1
pkgrel=1
pkgdesc="CachyOS kernel variants. You almost certainly do NOT want all six of these on a real install — pick your main kernel + maybe an -lts fallback, and only include -zfs/-nvidia-open variants if you actually need those features (they exist as separate builds for ABI reasons)."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  linux-cachyos
  linux-cachyos-nvidia-open
  linux-cachyos-lts
  linux-cachyos-lts-nvidia-open
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
