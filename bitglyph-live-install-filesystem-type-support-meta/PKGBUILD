# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-live-install-filesystem-type-support-meta
pkgver=1
pkgrel=1
pkgdesc="Support for filesystem types you're unlikely to run as your actual root fs, but that the live/install environment needs to read, write, or format during setup or when handling external media."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  exfatprogs
  f2fs-tools
  jfsutils
  nilfs-utils
  udftools
  mtools
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
