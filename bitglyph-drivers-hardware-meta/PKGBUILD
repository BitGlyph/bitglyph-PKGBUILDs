# GENERATED FILE -- do not edit directly.
# Regenerated by ci/generate_meta_packages.py from manifest.yaml.
pkgname=bitglyph-drivers-hardware-meta
pkgver=1
pkgrel=1
pkgdesc="Hardware enablement — GPU, USB, storage diagnostics, misc chip support."
arch=('any')
url="https://github.com/BitGlyph/bitglyph-PKGBUILDs"
license=('MIT')
depends=(
  mesa
  nvidia-utils
  bolt
  dmidecode
  usbutils
  usb_modeswitch
  usbmuxd
  libusb-compat
  pcsclite
  smartmontools
  sdparm
  sg3_utils
  nvme-cli
  lsscsi
)

package() {
  : # nothing to install, this exists purely to pull in dependencies
}
